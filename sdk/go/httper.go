package weknora

// httper.go intentionally left as a stub marker for future expansion. The
// SDK currently relies on *Client directly rather than a smaller interface,
// which keeps the surface concise. When we add a pluggable transport (e.g.
// grpc gateway) this file will host the HTTPDoer-style interface.
